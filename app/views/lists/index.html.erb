<div class="container">
  <div class="banner">
    <div class="content">
      <h1>Liv'Liste</h1>
      <p>Pour faire vos listes de bouquins à lire, sans en perdre une page.</p>
    </div>
  </div>
  <h1>Toutes vos listes</h1>
  <ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list_path(list) %></li>
  <% end %>
  </ul>
  <div class="btn-submit"><%= link_to "Créer une liste", new_list_path %></div>
</div>
